# Flask + DB

Flask

Flask-Cors

Flask-SQLAlchemy

Flask-Limiter

SQLAlchemy

python-dotenv

gunicorn

cryptography



# Vector Database
# Pinned to a minor range: chromadb's on-disk format changes between minors
# (a 0.4 index is unreadable by 0.5 — fails with KeyError '_type').
chromadb>=0.5.11,<0.6



# ML / Analytics

numpy

pandas

scikit-learn

openai



# PDF/Image processing

pypdfium2  # renders PDF pages; bundles pdfium, no Poppler needed
python-dateutil

Pillow



# API + Utils

requests

typing_extensions



# Auth (Supabase JWT verification)
PyJWT[crypto]

# Migrations + tests
alembic
pytest

# Postgres (required when DATABASE_URL is set)
psycopg2-binary

# Email (optional)

email-validator

imapclient

